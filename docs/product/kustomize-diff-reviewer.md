# Kustomize Output Diff Reviewer

## Summary

Compare two rendered Kustomize manifest bundles locally. Reports added, removed,
and changed resources with coarse categories (image, probes, resources, exposure).

## Route

`/tools/kustomize-output-diff`
